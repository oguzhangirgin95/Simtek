import { FlowConfig } from "../../../../../lib/base/baseconfig/config";

export const DashboardConfig: FlowConfig = {
  config: {
    steps: [
      {
        step: 'start',
        validation: [],
      }
    ],
  }
};