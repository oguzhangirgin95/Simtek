import { FlowConfig } from "../../../../../lib/base/baseconfig/config";

export const VehiclelistConfig: FlowConfig = {
  config: {
    steps: [
      {
        step: 'start',
        showContinueButton: false,
        showBackButton: false,
        validation: [],
      }
    ],
  }
};
